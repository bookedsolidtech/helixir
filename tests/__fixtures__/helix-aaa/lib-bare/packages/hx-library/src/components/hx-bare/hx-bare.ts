// Fixture: bare component — none of the AAA contract surface present.
import { LitElement } from 'lit';

export class HxBare extends LitElement {
  // intentionally no formAssociated, no attachInternals, no setValidity
}
