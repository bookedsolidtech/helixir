# Phase 4: Framework Wrapper Generator

*Part of: HELiXiR Platform — Programmatic API, CLI, GitHub Action, Wrapper Generator, VS Code Extension*

packages/wrapper-gen/ published as @helixir/wrapper-gen. CLI: helixir generate react --out ./wrappers/. React first (forwardRef, event binding, typed props from CEM, slot children). Then Vue, Angular, Svelte. Health annotations, deprecation warnings. Watch mode.

**Status:** pending

## Phases
