---
'helixir': patch
'@helixir/core': patch
---

Add antiPatterns array to styling_preflight output — agents get component-specific "don't do this" examples alongside validation results in a single call
