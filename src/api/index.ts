/**
 * Programmatic API — re-exports the full @helixir/core surface so consumers
 * can `import { ... } from 'helixir/api'` without pulling in MCP or CLI code.
 */
export * from '../../packages/core/src/index.js';
