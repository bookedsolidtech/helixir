/**
 * Programmatic API — re-exports the full @wc-tools/core surface so consumers
 * can `import { ... } from 'wc-tools/api'` without pulling in MCP or CLI code.
 */
export * from '../../packages/core/src/index.js';
