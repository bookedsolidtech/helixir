# Phase 1: Programmatic API Export

*Part of: HELiXiR Platform — Programmatic API, CLI, GitHub Action, Wrapper Generator, VS Code Extension*

Restructure src/ into api/, mcp/, cli/ directories. Create barrel exports for all handlers. Add exports map to package.json. Convert to monorepo with packages/core/. Ensure npx helixir still starts MCP server. All existing tests pass from new locations.

**Status:** pending

## Phases
